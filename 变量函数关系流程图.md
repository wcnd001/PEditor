# PEditor 3.0 主界面深度优化变量函数关系流程图

> 本文件用于记录本次基于当前上传代码进行的主界面优化。后续继续改动前，应先参考本文件，尤其注意模板选择固定首行、输入项行控件、复制按钮横向滚动卡片之间的联动关系。

## 1. 主界面总体结构

```mermaid
flowchart TB
    PEditor[PEditor 主窗口] --> toolbar[create_toolbar: QToolBar]
    PEditor --> main_area[create_main_area: QSplitter 主区域]

    toolbar --> file_menu[文件菜单: 新建/重命名/删除/导入/导出/TXT/退出]
    toolbar --> edit_menu[编辑菜单: 添加选项行/删除选中/编辑名称/输入项配置/工序模板/规则/数据库/清空]
    toolbar --> browser_menu[浏览器菜单: 浏览器参数/打开浏览器/浏览器配置/导出至浏览器]
    toolbar --> copy_menu[复制菜单: 添加/删除/前移/后移/多选]
    toolbar --> help_menu[帮助菜单: 界面设置/教程/日志]

    main_area --> splitter1[splitter1 垂直分割: 上方输入+预览 / 下方复制]
    splitter1 --> splitter2[splitter2 水平分割: 左上选项 / 右上预览]
    splitter2 --> left_panel[create_left_panel: 选项滚动面板]
    splitter2 --> right_panel[create_right_panel: 预览框]
    splitter1 --> down_panel[create_down_panel: 复制按钮横向滚动面板]
```

## 2. 模板选择固定首行

```mermaid
flowchart LR
    create_left_panel[create_left_panel] --> template_combo[template_combo]
    template_combo --> template_row[TemplateSelectRow 固定首行]
    template_row --> input_layout[input_layout]
    template_combo --> on_template_changed[on_template_changed]
    on_template_changed --> _load_template_by_name[_load_template_by_name]
    on_template_changed --> refresh_input_area[refresh_input_area]
    on_template_changed --> update_copy_buttons_from_config[update_copy_buttons_from_config]
    on_template_changed --> update_result_text[update_result_text]
```

### 说明

- `template_combo` 已从 `QToolBar` 移入左侧选项面板。
- `TemplateSelectRow` 是默认固定第一行，不参与拖拽排序、不参与 `collect_input_values()`。
- 顶部工具栏不再显示模板选择框。
- 浏览器参数用到的 `browser_driver_edit`、`browser_binary_edit`、`browser_url_edit` 仍作为状态控件存在，但已隐藏，避免主界面左上角出现无意义输入框。

## 3. 输入选项区流程

```mermaid
flowchart TB
    current_options_config[current_options_config] --> refresh_input_area[refresh_input_area]
    refresh_input_area --> _clear_input_option_rows[_clear_input_option_rows 只清理输入行]
    _clear_input_option_rows --> keep_template_row[保留 TemplateSelectRow 固定首行]
    refresh_input_area --> _create_widget[_create_widget]
    _create_widget --> InputOptionRow[InputOptionRow]
    InputOptionRow --> option_rows[option_rows]
    InputOptionRow --> input_widgets[input_widgets]

    InputOptionRow --> select_option_row[select_option_row]
    select_option_row --> selected_option_row[selected_option_row]
    selected_option_row --> set_selected[set_selected 选中变色]

    InputOptionRow --> move_option_row_by_mouse[move_option_row_by_mouse]
    move_option_row_by_mouse --> sync_option_order_from_rows[sync_option_order_from_rows]
    sync_option_order_from_rows --> current_options_config
    sync_option_order_from_rows --> _save_current_template[_save_current_template]

    input_widgets --> on_input_widget_changed[on_input_widget_changed]
    on_input_widget_changed --> refresh_dynamic_combo_options[refresh_dynamic_combo_options]
    on_input_widget_changed --> update_result_text[update_result_text]
```

### 关键变量影响

| 变量 | 作用 | 改动影响 |
|---|---|---|
| `template_combo` | 当前模板选择，位于固定首行 | 影响当前模板切换、输入项重建、复制按钮重建、预览刷新 |
| `template_row` | 左侧滚动面板的固定第一行 | 不参与排序，不参与输入值采集 |
| `current_options_config` | 当前模板的输入项配置 | 影响输入项生成、顺序保存、规则字段池、模板保存 |
| `option_rows` | 当前显示的输入行对象 | 影响选择高亮、拖拽排序、输入值采集 |
| `selected_option_row` | 当前选中的输入行 | 影响删除选中、编辑选项名称 |
| `input_widgets` | 输入控件列表 | 影响输入变化监听、清空输入、动态下拉项刷新 |

## 4. 输入行选中变色修正

```mermaid
flowchart LR
    click[点击 InputOptionRow / 标签 / 输入框] --> select_option_row[select_option_row]
    select_option_row --> same{是否同一行}
    same -->|是且允许切换| unselect[取消选中]
    same -->|否| clear_old[旧行 set_selected False]
    clear_old --> select_new[新行 set_selected True]
    select_new --> stylesheet[更新行背景色和边框]
```

### 说明

- `InputOptionRow.set_selected()` 改为直接设置当前行和拖动柄样式。
- 选中状态有蓝色边框和浅蓝背景。
- 再次点击同一行会取消选中；点击输入控件时会保持选中。

## 5. 复制按钮横向滚动区

```mermaid
flowchart TB
    config_copy_buttons[主模板 config.copy_buttons] --> update_copy_buttons_from_config[update_copy_buttons_from_config]
    update_copy_buttons_from_config --> _rebuild_copy_button_widgets[_rebuild_copy_button_widgets]
    _rebuild_copy_button_widgets --> CopyButtonItem[CopyButtonItem]
    CopyButtonItem --> copy_buttons[copy_buttons: CopyButtonItem + field_name]
    CopyButtonItem --> copy_buttons_layout[copy_buttons_layout: QHBoxLayout]
    copy_buttons_layout --> copy_scroll[copy_scroll 横向滚动]

    CopyButtonItem --> handle[顶部拖动柄]
    handle --> move_copy_item_by_mouse[move_copy_item_by_mouse]
    move_copy_item_by_mouse --> sync_copy_order_from_items[sync_copy_order_from_items]
    sync_copy_order_from_items --> copy_buttons
    sync_copy_order_from_items --> _save_current_template[_save_current_template]

    CopyButtonItem --> button[复制按钮]
    button --> on_copy_button_clicked[on_copy_button_clicked]
    on_copy_button_clicked --> copy_field_content[copy_field_content]
    copy_field_content --> clipboard[系统剪贴板]
```

### 复制按钮区设计

- `copy_scroll` 使用横向滚动，不再强制把内容压缩到视口宽度。
- 每个复制项由 `CopyButtonItem` 表示。
- 每个复制项纵向排列：顶部为拖动排序标记 `☰`，下方为复制按钮。
- 多个复制项整体横向排列，按钮较多时通过横向滚动查看。
- 复制按钮仍然只读取主模板 `copy_buttons`，不会从字段池自动恢复，避免删除后刷新又出现。

## 6. 复制按钮选择、多选、拖拽和保存

```mermaid
flowchart LR
    click_button[点击复制按钮] --> on_copy_button_clicked[on_copy_button_clicked]
    on_copy_button_clicked --> multi{多选?}
    multi -->|否| single_select[只保留当前选中]
    multi -->|是| multi_select[追加或移除当前项]
    single_select --> _apply_copy_button_checked_state[_apply_copy_button_checked_state]
    multi_select --> _apply_copy_button_checked_state
    _apply_copy_button_checked_state --> CopyButtonItemStyle[CopyButtonItem 选中变色]

    drag_handle[拖动顶部排序标记] --> move_copy_item_by_mouse[move_copy_item_by_mouse]
    move_copy_item_by_mouse --> sync_copy_order_from_items[sync_copy_order_from_items]
    sync_copy_order_from_items --> get_current_copy_button_fields[get_current_copy_button_fields]
    get_current_copy_button_fields --> _save_current_template[_save_current_template]
```

## 7. 预览与导出流程

```mermaid
flowchart TB
    input_change[输入项变化] --> collect_input_values[collect_input_values]
    collect_input_values --> data_matcher[DataMatcher.render]
    data_matcher --> result_text_value[完整工艺文本]
    data_matcher --> data_pool[中间字段池]
    data_matcher --> final_fields[最终字段内容]

    result_text_value --> _set_result_text_preserve_view[_set_result_text_preserve_view]
    _set_result_text_preserve_view --> result_text[右上预览框]
    final_fields --> refresh_copy_button_visibility[refresh_copy_button_visibility]

    result_text --> save_to_summary_file[导出工艺TXT]
    final_fields --> export_current_to_browser[导出至浏览器]
    result_text --> export_current_to_browser
```

## 8. 浏览器参数流程

```mermaid
flowchart LR
    browser_menu[浏览器菜单] --> open_browser_settings_dialog[浏览器参数]
    open_browser_settings_dialog --> browser_driver_edit[browser_driver_edit 隐藏状态控件]
    open_browser_settings_dialog --> browser_binary_edit[browser_binary_edit 隐藏状态控件]
    open_browser_settings_dialog --> browser_url_edit[browser_url_edit 隐藏状态控件]
    browser_url_edit --> normalize_main_browser_url_input[normalize_main_browser_url_input]
    normalize_main_browser_url_input --> _save_browser_settings_for_current_template[_save_browser_settings_for_current_template]
    _save_browser_settings_for_current_template --> template_db_browser_flow[template_db.browser_flows]
```

## 9. 后续修改注意事项

1. 不要再把 `template_combo` 放回工具栏；模板选择固定在左侧选项面板第一行。
2. 不要让 `_clear_input_option_rows()` 删除 `TemplateSelectRow`。
3. 不要把 `browser_driver_edit`、`browser_binary_edit`、`browser_url_edit` 加入主界面布局；它们只是隐藏状态控件。
4. 复制按钮区应继续使用 `CopyButtonItem`，保证顶部拖动柄和横向滚动逻辑不丢失。
5. 修改复制按钮排序时，应同步调用 `sync_copy_order_from_items(save=True)` 或 `_save_current_template()`。
6. 修改输入项排序时，应同步调用 `sync_option_order_from_rows(save=True)`。

## 8. 本次补充优化：模板固定行、选中宽度、测试数据库

```mermaid
flowchart TB
    TemplateSelectRow[TemplateSelectRow 固定首行] --> template_combo[template_combo 模板选择框]
    TemplateSelectRow --> no_fixed_text[删除“固定”文字，仅保留空白对齐位]
    InputOptionRow[InputOptionRow 输入选项行] --> update_label_max_width[update_label_max_width]
    update_label_max_width --> fixed_label_width[setFixedWidth 固定标签宽度]
    fixed_label_width --> stable_click[点击选中/取消只变色，不改变输入框宽度]
    TestDB[测试数据] --> data_db[data.db: 产品基础数据/胶料数据库]
    TestDB --> template_db[template.db: 测试模板-PEditor3.0]
    template_db --> test_options[输入选项配置]
    template_db --> test_rules[规则配置]
    template_db --> test_fields[字段模板内容]
    template_db --> test_copy_buttons[复制按钮布局 copy_buttons]
    template_db --> test_browser_flow[浏览器流程示例]
```

### 8.1 模板固定首行调整

- `TemplateSelectRow` 中删除了可见的“固定”两个字。
- 为了保持模板行与普通输入行左侧对齐，保留了一个空白占位控件，但该控件不显示文字、不显示边框。
- `template_combo` 仍然是固定第一行，不参与 `collect_input_values()`，也不参与拖拽排序。

### 8.2 选项点击后宽度变化的处理

- 原因：输入行点击选中时会刷新样式，若标签只设置 `maximumWidth`，布局可能重新计算标签和输入框的可用宽度，造成视觉上的宽度跳动。
- 处理：`InputOptionRow.update_label_max_width()` 改为按左侧滚动面板宽度计算 `label_width`，并使用 `self.label.setFixedWidth(label_width)` 固定标签宽度。
- 结果：点击选中/取消选中只改变背景色和边框色，不再改变输入框宽度。

### 8.3 测试数据库与模板

本次生成了两份测试文件，放在程序同级目录，便于直接启动后测试：

| 文件 | 内容 | 用途 |
|---|---|---|
| `data.db` | `产品基础数据`、`胶料数据库` 两张测试数据表 | 测试下拉数据源、数据库查询规则 |
| `template.db` | `测试模板-PEditor3.0` | 测试输入项、规则、字段模板、复制按钮布局、浏览器流程示例 |

测试模板包含：

- 输入项：`产品型号`、`牌号`、`数量`、`操作者`、`是否二段硫化`。
- 规则：产品型号查工艺类型/温度/工时，牌号查胶料说明/质量关注点，数量计算抽检数量，复选框生成二段硫化要求。
- 字段模板：`基本信息`、`胶料检查`、`检验要求`、`二段硫化段落`、`浏览器填报摘要`。
- 复制按钮布局：按 `基本信息 → 胶料检查 → 检验要求 → 二段硫化段落 → 浏览器填报摘要` 横向排列。
- 浏览器流程：包含 3 个示例输入步骤，定位值需要按真实网页修改。

## 9. 字体与紧凑程度设置优化

```mermaid
flowchart TB
    open_settings[open_settings 打开界面设置] --> UiSettingsDialog[UiSettingsDialog]
    UiSettingsDialog --> option_setting[选项字体/选项紧凑数字]
    UiSettingsDialog --> toolbar_setting[工具栏字体/工具栏紧凑数字]
    UiSettingsDialog --> copy_setting[复制按钮字体/复制按钮紧凑数字]
    UiSettingsDialog --> preview_setting[预览框字体/预览框紧凑数字]

    option_setting --> apply_ui_settings[apply_ui_settings]
    toolbar_setting --> apply_ui_settings
    copy_setting --> apply_ui_settings
    preview_setting --> apply_ui_settings

    apply_ui_settings --> _build_app_stylesheet[_build_app_stylesheet 生成分类QSS]
    apply_ui_settings --> _apply_runtime_ui_settings[_apply_runtime_ui_settings 控件级应用字体]

    _apply_runtime_ui_settings --> main_toolbar[main_toolbar / toolbar_buttons]
    _apply_runtime_ui_settings --> template_row[TemplateSelectRow]
    _apply_runtime_ui_settings --> option_rows[InputOptionRow 列表]
    _apply_runtime_ui_settings --> copy_buttons[CopyButtonItem 列表]
    _apply_runtime_ui_settings --> result_text[result_text 预览框]
```

### 9.1 设置项变更

原来只有一个 `font_size` 和一个文字型 `button_compactness`，现在改为 8 个独立数字项：

| 设置键 | 控制范围 | 说明 |
|---|---|---|
| `option_font_size` | 左侧输入选项区 | 控制模板固定行、选项标签、输入框/下拉框字体 |
| `option_compactness` | 左侧输入选项区 | 数字越小越紧凑，影响选项行边距、间距、行高 |
| `toolbar_font_size` | 顶部工具栏 | 控制 `QToolBar/QToolButton/QMenu` 字体 |
| `toolbar_compactness` | 顶部工具栏 | 数字越小越紧凑，影响工具栏菜单按钮高度和内边距 |
| `copy_font_size` | 下方复制按钮区 | 控制复制按钮卡片、拖动柄、复制按钮字体 |
| `copy_compactness` | 下方复制按钮区 | 数字越小越紧凑，影响复制卡片边距、拖动柄高度、按钮高度 |
| `preview_font_size` | 右上预览框 | 控制工艺预览文本字号 |
| `preview_compactness` | 右上预览框 | 控制预览框文本内边距 |

### 9.2 字体设置不生效的处理

本次不再只依赖全局 `QApplication.setStyleSheet()`，而是同时使用两种方式：

1. `_build_app_stylesheet()` 生成分类 QSS，分别控制工具栏、选项行、复制按钮和预览框。
2. `_apply_runtime_ui_settings()` 直接对已存在控件调用 `setFont()` 和 `setMinimumHeight()`，避免 `InputOptionRow.set_selected()`、`CopyButtonItem.set_selected()` 的局部样式覆盖导致字体设置不稳定。

后续如果新增主界面控件，需要根据控件所属区域加入对应分类，否则可能不受界面设置控制。

### 9.3 预览框边框隐藏

- `result_text` 设置 `objectName='previewTextEdit'`。
- `create_right_panel()` 中对 `result_text` 调用 `setFrameShape(QFrame.NoFrame)`。
- `_build_app_stylesheet()` 与 `_apply_runtime_ui_settings()` 均设置 `border: none`，确保预览框内部文本框无边框显示。

## 9. 2026-05-14 字体设置闪退修复

```mermaid
flowchart TB
    open_settings[open_settings 打开界面设置] --> UiSettingsDialog[UiSettingsDialog 四类字体/紧凑数字]
    UiSettingsDialog --> get_settings[get_settings 获取设置]
    get_settings --> normalize[_normalize_ui_settings 规范化数值范围]
    normalize --> apply[apply_ui_settings]
    apply --> window_qss[self.setStyleSheet 仅作用主窗口]
    apply --> runtime[_apply_runtime_ui_settings 控件级刷新]
    runtime --> toolbar[工具栏按钮/菜单字体]
    runtime --> template_row[模板固定行字体]
    runtime --> option_rows[选项行字体与高度]
    runtime --> copy_items[复制按钮项字体与高度]
    runtime --> preview[预览框字体与隐藏边框]
    apply --> save_settings[save_settings 保存 settings.json]
```

### 本次闪退原因防护

- `apply_ui_settings()` 不再直接使用 `QApplication.setStyleSheet()` 影响整个应用，改为 `self.setStyleSheet()`，避免设置对话框关闭过程中被全局样式刷新牵连。
- `_apply_runtime_ui_settings()` 对工具栏、模板行、选项行、复制按钮、预览框分别容错刷新；如果某个控件已经被 `deleteLater()` 标记或被 Qt 删除，会跳过该控件，不再让异常冒泡导致主程序退出。
- `open_settings()` 增加异常捕获，应用设置失败时弹窗提示并打印 traceback，不再直接闪退。
- `copy_buttons` 遍历兼容 `[(CopyButtonItem, field_name)]` 和旧中间版本 `[CopyButtonItem]` 两种残留结构，避免解包异常。
- 预览框继续使用 `QFrame.NoFrame`、`lineWidth=0`、`viewport` 无边框样式隐藏边框。
